<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonGotoLast"
        android:drawableStart="@android:drawable/ic_menu_revert"
        android:layout_gravity="center_horizontal"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <SeekBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/playerSeekBar"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignBottom="@+id/buttonGotoLast"
        android:layout_toLeftOf="@+id/playPauseButton"
        android:layout_toStartOf="@+id/playPauseButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/playPauseButton"
        android:text=""
        android:minHeight="50dp"
        android:minWidth="50dp"
        android:contentDescription="@string/play_pause_button_content_description"
        android:layout_alignBottom="@+id/buttonGotoLast"
        android:layout_toLeftOf="@+id/buttonGotoLast"
        android:layout_toStartOf="@+id/buttonGotoLast">
    </Button>

</RelativeLayout>