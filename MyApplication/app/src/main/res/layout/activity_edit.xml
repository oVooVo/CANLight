<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="1">

    <RelativeLayout
        android:id="@+id/playerLayout"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:gravity="center_vertical">

        <SeekBar
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:id="@+id/playerSeekBar"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="false"
            android:layout_toLeftOf="@+id/playerPlayPauseButton"
            android:layout_toStartOf="@+id/playerPlayPauseButton" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:minHeight="50dp"
            android:minWidth="50dp"
            android:contentDescription="@string/play_pause_button_content_description"
            android:id="@+id/playerPlayPauseButton"
            android:layout_alignParentTop="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true" />
        <!--android:drawableStart="@android:drawable/ic_media_play"-->

    </RelativeLayout>

    <com.example.pascal.canlight.AutoScrollView
        android:id="@+id/autoScrollView"
        android:layout_width="match_parent"
        android:layout_gravity="top"
        android:layout_height="350dp"
        android:layout_alignParentTop="true"
        android:layout_marginTop="0dp"
        android:layout_above="@+id/playerLayout">
        <com.example.pascal.canlight.ChordPatternEdit
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:inputType="textMultiLine"
            android:id="@+id/editText"
            android:windowSoftInputMode="adjustPan"/>
    </com.example.pascal.canlight.AutoScrollView>

</RelativeLayout>